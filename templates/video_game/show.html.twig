{% extends 'base.html.twig' %}

{% block title %}VideoGame{% endblock %}

{% block body %}
    <h1>VideoGame</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video_game.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ video_game.title }}</td>
            </tr>
            <tr>
                <th>ReleaseDate</th>
                <td>{{ video_game.releaseDate ? video_game.releaseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ video_game.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_video_game_index') }}">back to list</a>

    <a href="{{ path('app_video_game_edit', {'id': video_game.id}) }}">edit</a>

    {{ include('video_game/_delete_form.html.twig') }}
{% endblock %}
